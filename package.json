{
  "name": "projeto",
  "version": "1.0.0",
  "description": "Projeto FullStack com Frontend e Backend",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run start-backend\" \"npm run start-frontend\"",
    "start-backend": "cd app/Backend && npm start",
    "start-frontend": "cd app/Frontend && npm start",
    "install-all": "npm install && cd app/Backend && npm install && cd ../Frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}